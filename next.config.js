/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["zzoewvnlemhnbzkfiwvc.supabase.co", "images.unsplash.com"],
  },
};

module.exports = nextConfig;
